<div id="myModal" class="modal">

  <!-- Modal content -->
  <div class="modal-content">
    <span class="close">&times;</span>
    <div id="modalMain">
      <% if (typeof user_id == 'undefined')  {%>
        <button class="registerModalBtn">Register</button>
        <button class="loginModalBtn">Login</button>
      <% } else { %>
        <button class="logoutModalBtn">Logout</button>
      <% } %>
    </div>
  </div>

</div>